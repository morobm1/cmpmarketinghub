.env


# Local Netlify folder
.netlify

# RV source Excel files (contain PII)
RV/*.xlsx
RV/*.xls

# Reslife Hub reference docs (contain staff names/budget details; local reference only)
RA_Harbour/

# Email Shop build artifacts (rebuilt on deploy)
email-shop/node_modules/
email-shop/src/examples/
# ONNX runtime WASM files are huge (~24MB) and loaded from CDN at runtime
email-shop/dist/assets/*.wasm
email-shop/dist/assets/*.mjs
